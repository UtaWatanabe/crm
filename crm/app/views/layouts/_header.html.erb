<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse-menu">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/customers">顧客管理システム</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navbar-collapse-menu">
      <ul class="nav navbar-nav navbar-right">
      <!-- viewの中でログイン時､と未ログイン時で表示を変える-->
      <% if user_signed_in? %>
      <!-- signed_inはdeviseのヘルパーメソッド -->
      <!-- ?をつけるのはtrueかfalseかのいずれかを返すときの慣用句 -->
        <li><%= link_to 'ログアウト', destroy_user_session_path, method: :delete %></a></li>
      <% else %>
        <li><%= link_to '新規登録', new_user_registration_path %></li>
        <li><%= link_to 'ログイン', user_session_path, method: :post %></li>
      <% end %>
      <!-- %=では無いのは表示させる必要が無い記述だから -->

      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
